<Controls:MetroWindow x:Class="DotNetMonitor.UI.Views.ProcessDetailView"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                      xmlns:local="clr-namespace:DotNetMonitor.UI.Views"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                      Title="Process Detail View"
                      Width="800"
                      Height="450"
                      mc:Ignorable="d">
    <Grid Margin="2,5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TabControl Grid.Row="1">
            <TabItem Header="Basic">
                <WrapPanel Orientation="Vertical">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Process ID: " />
                        <TextBlock Grid.Column="1"
                                   Text="{Binding ProcessId}" />
                    </Grid>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="GC Mode: " />
                        <TextBlock Grid.Column="1"
                                   Text="{Binding GCMode}" />
                    </Grid>
                </WrapPanel>
            </TabItem>
            <TabItem Header="Modules">
                <DataGrid AutoGenerateColumns="False"
                          ItemsSource="{Binding Modules}">

                    <DataGrid.Columns>
                        <DataGridTextColumn Binding="{Binding Name}"
                                            Header="Name" />
                        <DataGridTextColumn Binding="{Binding Version}"
                                            Header="Version" />
                        <DataGridTextColumn Binding="{Binding Path}"
                                            Header="Path" />
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
    </Grid>
</Controls:MetroWindow>
